name: POCworkflow
on:
  workflow_dispatch

jobs:
  my-job:
    uses: Akash-HM/testrepo2/.github/workflows/main.yml@main
    with:
      runner: 'ubuntu-latest'
      checkoutrepo: 'Akash-HM/testrepo2'
      checkoutbranch: 'main'
      node-version: '12.x'
